/**
 * Modal markup owned by this page. Superseded by the real React modals in
 * SubmitIngestModal.tsx / BulkUpdateModal.tsx (both now hit the live API) —
 * kept empty rather than deleted so the shared modals.ts aggregator import
 * still resolves.
 */
export const modalsHtml = "";
