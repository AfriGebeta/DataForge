import type { PageConfig } from '@/features/shared/types';

export const pageConfig = {
  "id": "informal-areas",
  "label": "Informal Areas",
  "title": "Informal Areas",
  "icon": "ti-map-pin",
  "slug": "informal-areas",
  "path": "/address/informal-areas",
  "apiEndpoint": "/api/v1/informal-areas"
} satisfies PageConfig;
